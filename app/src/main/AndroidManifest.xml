<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pmegplogo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/App"
        tools:targetApi="31" >

        <activity
            android:name=".activity.LaunchScreenActivity"
            android:exported="true"
            android:theme="@style/App" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.screens.LoginActivity"
            android:theme="@style/App"/>

        <activity android:name=".activity.screens.DashboardScreenActivity"
            android:theme="@style/App"/>

        <activity android:name=".activity.screens.NewApplicantUnitActivity"
            android:label="Application For New Unit"
            android:theme="@style/App"/>

        <activity android:name=".activity.screens.RegisteredApplicantActivity"
            android:theme="@style/App"/>


    </application>
    <queries>
        <!-- Allow your app to see AadhaarFaceRD -->
        <package android:name="in.gov.uidai.facerd.nonprod"/>
        <!-- Also allow querying its capture intent -->
        <intent>
            <action android:name="in.gov.uidai.rdservice.face.CAPTURE" />
        </intent>
    </queries>
</manifest>